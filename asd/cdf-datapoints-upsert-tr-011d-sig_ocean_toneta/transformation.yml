---
externalId: cdf:datapoints:upsert:tr:011d:sig_ocean_toneta
name: cdf:datapoints:upsert:tr:011d:sig_ocean_toneta
shared: true

schedule: "25,55 * * * *"

query:
    file:
        transformation.sql
action: upsert
destination: datapoints

notifications:
    - aaaaez5frjt3ah5lo5hdext7de@cognitedata.slack.com
authentication:
    tokenUrl: ${TOKEN_URL}
    scopes:
        - ${SCOPES}
    cdfProjectName: ${COGNITE_PROJECT}
    clientId: ${COGNITE_CLIENT_ID}
    clientSecret: ${COGNITE_CLIENT_SECRET}
...
