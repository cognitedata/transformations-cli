---
externalId: cdf:event:delete:tr:010c:sig_ocean_evdetails
name: cdf:event:delete:tr:010c:sig_ocean_evdetails
shared: true

# schedule: "1/15 * * * *"

query:
    file:
        transformation.sql
action: delete
destination: events

notifications:
    - aaaaez5frjt3ah5lo5hdext7de@cognitedata.slack.com
authentication:
    tokenUrl: ${TOKEN_URL}
    scopes:
        - ${SCOPES}
    cdfProjectName: ${COGNITE_PROJECT}
    clientId: ${COGNITE_CLIENT_ID}
    clientSecret: ${COGNITE_CLIENT_SECRET}
...
